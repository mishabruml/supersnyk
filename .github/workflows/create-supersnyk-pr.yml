name: Create SuperSnyk 🦸 Pull Request
on:
  # workflow_dispatch:
  push

jobs:
  create-supersnyk-pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: main
          fetch-depth: 0
      - uses: bobvanderlinden/combine-pull-requests@v3
        with:
          label: snyk
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Get Snyk PR metadata
        run: ./.github/get-snyk-prs.sh
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Use the value
        id: step_two
        run: echo "${{ env.SUPERSNYK_PR_BODY }}"
      - name: Create SuperSnyk Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          title: Supersnyk 🦸
